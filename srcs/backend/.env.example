# JWT Secret Key (REQUIRED)
# Utilisé pour signer les tokens JWT
# Générer avec: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here

# Google OAuth (OPTIONAL - pour OAuth Google)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Email Configuration for 2FA (REQUIRED for 2FA feature)
# Utilisez un compte Gmail avec un "App Password"
# Pour créer un App Password:
# 1. Activez la 2FA sur votre compte Google
# 2. Allez sur https://myaccount.google.com/apppasswords
# 3. Créez un nouveau App Password
EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASSWORD=your-16-character-app-password

# Database (OPTIONAL - par défaut: /app/db/pong.db)
# DATABASE_PATH=/app/db/pong.db
